# Backend API for the Stock Market Investment Club dashboard.
# Install:  pip install -r requirements.txt
# Run:      uvicorn api:app --reload --port 8000

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
yfinance>=0.2.54
curl_cffi>=0.7.0
pandas>=2.0.0
numpy>=1.24.0

# Trading game: database + signed sessions (username/password auth)
sqlalchemy>=2.0.0
itsdangerous>=2.1.0
psycopg2-binary>=2.9.0   # Postgres driver for production (DATABASE_URL)
